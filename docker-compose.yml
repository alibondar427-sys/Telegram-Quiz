version: '3.8'
services:
  bot:
    build: .
    environment:
      - TG_BOT_TOKEN=${TG_BOT_TOKEN}
      - ADMIN_IDS=${ADMIN_IDS}
      - DATABASE_PATH=/data/quiz_bot.db
    volumes:
      - ./data:/data
